---
driver:
  name: docker
docker:
  containers:
    - name: ansible-pam_yubikey-1604
      image: ubuntu
      image_version: 16.04
verifier:
  name: testinfra

ansible:
  playbook: tests/playbook.yml
