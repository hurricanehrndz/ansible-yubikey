---
galaxy_info:
  author: Carlos Hernandez
  description: Configure pam to use yubikey OTP + password for sshd, additionally configure sudo to use yubikey OTP for authentication alone.
  license: MIT
  platforms:
    - name: Ubuntu
      versions:
      - xenial
  min_ansible_version: 1.8
  galaxy_tags:
    - system
    - pam
    - yubikey
dependencies:
  - role: ansible-openssh
    src: git+https://github.com/hurricanehrndz/ansible-openssh
    version: master
